export const constants = {
    maxVotingWaitingTimeSecs: 600,
    // Maximum waiting time for an order.
    // In seconds (10 minutes)
    maxWaitingTimeSecs: 600,
    // Maximum file size that can be accepted by the bot.
    // In bytes (20 MB)
    maxFileSize: 20 * 1024 * 1024,
    // Maximum number of people
    maxPeopleInOrder: 4,
    // Minimum number of people
    minPeopleInOrder: 1,
    // ms
    registerMessagesDelay: 700,
    // ms
    orderMessageDelay: 500,
    // ms
    rateMessagesDelay: 500,
};
