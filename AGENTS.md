# Codex Project Notes

Before working in Codex Cloud, read `docs/CODEX_CLOUD_CONTEXT.md`.

This repository contains an in-progress FSM adaptation for a taxi ordering
service. The important prior context is not guaranteed to arrive from the local
Codex thread, so treat `docs/CODEX_CLOUD_CONTEXT.md` as the handoff source of
truth before planning or editing.

